'use strict';

/**
 *  dungeon-master controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::dungeon-master.dungeon-master');
